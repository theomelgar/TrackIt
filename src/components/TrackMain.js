import { BrowserRouter, Route, Routes } from "react-router-dom";

export default function TrackMain() {
    return (
        <BrowserRouter>
            <Routes>
                <Route/>
            </Routes>
        </BrowserRouter>
    )
}